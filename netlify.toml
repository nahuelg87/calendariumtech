[build]
  command = "npm run build"
  publish = "dist"

[functions]
  node_bundler = "esbuild"
  directory = ".netlify/v1/functions"

[[redirects]]
  from = "/*"
  to = "/.netlify/functions/ssr"
  status = 200