[build]
  command = "npm run build"
  publish = "dist"

# Eliminamos node_compatibility de acá porque da conflicto de sintaxis
[functions]
  directory = ".netlify/functions-internal"

[[redirects]]
  from = "/*"
  to = "/.netlify/functions/entry"
  status = 200